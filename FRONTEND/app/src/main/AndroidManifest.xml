<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kidskeeper.sungshin.or.kr.kikee">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- 블루투스 설정메뉴 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 안드로이드 메모리 가용 용량 증가(largeHeap) -->
    <application
        android:name=".Network.ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home.LoginActivity" />
        <activity android:name=".Adult.AdultHome.AdultHomeActivity" />
        <activity android:name=".Adult.Camera.CameraActivity" />
        <activity android:name=".Kids.Connect.ConnectActivity" />
        <activity android:name=".Kids.Operate.OperateRobot" />
        <activity android:name=".Kids.WordGame.CategorySelectActivity" />
        <activity android:name=".Kids.WordGame.PlayWordGameActivity" />
        <activity android:name=".Kids.WordGame.SuccessWord" />
        <activity android:name=".Adult.Community.BoardDetailActivity" />
        <activity android:name=".Adult.Community.BoardWriteActivity" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".Home.JoinActivity" />
        <activity android:name=".Kids.HomeWork.HomeWork" />
        <activity android:name=".Home.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Kids.KidsHome.KidsHomeActivity"></activity>
    </application>

</manifest>