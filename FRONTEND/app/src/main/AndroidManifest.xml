<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kidskeeper.sungshin.or.kr.kikee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdultLoginActivity" />
        <activity android:name=".Adult.AdultHome.AdultHomeActivity" />
        <activity android:name=".Adult.Camera.CameraActivity" />
        <activity android:name=".Kids.ConnectActivity" />
        <activity android:name=".Adult.Voice.VoiceActivity"></activity>
    </application>

</manifest>